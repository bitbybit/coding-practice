<p>Given an array of numbers and an integer target, find two unique numbers in the array that add up to the target value. Return an array with the indices of those two numbers, or <code>"Target not found"</code> if no two numbers sum up to the target.</p>
<ul>
<li>The returned array should have the indices in ascending order.</li>
</ul>
